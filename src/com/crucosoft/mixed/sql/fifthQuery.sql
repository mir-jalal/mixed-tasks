CREATE TABLE IF NOT EXISTS cp_students AS (SELECT * FROM students);

CREATE TABLE IF NOT EXISTS cp_teachers AS (SELECT * FROM teachers);

CREATE TABLE IF NOT EXISTS cp_courses AS (SELECT * FROM courses);

CREATE TABLE IF NOT EXISTS cp_assignments AS (SELECT * FROM assignments);
